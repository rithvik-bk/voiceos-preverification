# G1+G2 — Spec lock + walking skeleton (merged, async)
*Written 2026-08-21 ~17:30. Per the approved calendar this brief is async: the program proceeds on the recommendations below; reply only to overrule.*

## 1. What we built (three sentences)
SPEC.md is locked: 347 lines, 94 MUST clauses (S1.1–S22.2), with your ownership rule as the §0 claims-discipline preamble, and every ambiguity RECON's era of the doc allowed (deixis, fuzzy, barge-in) rendered as decision procedures; INVARIANTS.md holds I1–I16, each mapped to an EXISTS test (real paths in your repo) or a PLANNED one. The walking skeleton runs end to end in `packages/core`: transcript → grounding → gate → verdict → receipt → shadow log, ported from your proven gate, 9/9 tests, zero runtime deps, **7.1 µs clean / 19.0 µs blocked** full-pipeline mean. Amendment 3 is executed: the §13 screen-drift fixture catches the stale-screen end-time with the REAL gate's verdict (suite 418/418, additive-only, eval unchanged 50/50).

## 2. What works, with the commands
```
cd /Users/rithvik/preflight/packages/core && npm test && npm run bench   # 9/9, 7.1µs/19.0µs
cd /Users/rithvik/universal-voiceos-oauth && node tools/screen-drift-fixture.mjs   # §13: PASS
cd /Users/rithvik/universal-voiceos-oauth && npm test                    # 418/418 (416+2), no regressions
```

## 3. What broke or surprised us
- **The `said`-pattern caveat (most important):** the skeleton's rank-4 "transcript" is the *claimed* transcript — we consume what the platform hands us; nothing attests it to real audio. On stage this is framed as the §22/§28 adoption ask ("the platform holds the audio truth; expose it and rank 4 becomes attested"), never hidden.
- **Blocked path is ~2.7× slower than clean** (Levenshtein candidate list on miss). Microseconds either way; watch it at large directories, no action now.
- **Skeleton raised a design question — answered by the locked spec, no change made:** "should Tier-3 routing require a rank-4 co-source?" S11.2 already requires transcript-span licensing for the dangerous subset (destinations, amounts, permissions); other routing params (thread_ts from a prior read) are rank-3-eligible by design. Recommendation: keep as specified. Raising min-ranks further would be a spec change → your call, not ours.

## 4. Decision needed
None blocking. One recommendation to confirm passively: the S11.2 answer above stands unless you say otherwise.

## 5. What changes if you overrule
Requiring rank-4 co-sources on ALL Tier-3 routing params would false-block legitimate "reply to that thread" flows (thread_ts is never spoken aloud) — this is why we recommend against.

## 6. DEMO-READY status
Upgraded: the skeleton + §13 fixture + your 418-test integration are all runnable now (commands above). Act 1/Act 2 wiring is Phase 3.

## In flight at time of writing
REDTEAM-BLIND corpus (spec-only isolation) · CONTRACTS pod (§20 annotations + CI lint + coverage counter) · ACT-1 pod (transcript-span teeth + the threat model's must-verify negative).
THREATMODEL is DONE: 10 attack trees, every one cited to SPEC/INVARIANTS ids with named fixtures, honest about what does NOT survive a live founder attack (spatial injection + ASR-revision racing = harness-only — the platform exposes no screen/streaming events). Its load-bearing flag: the Act-1 injection block depends on `groundFromToolResult` (tools-t2.ts:269) never harvesting body text into routing referents — the negative is UNPROVEN until the ACT-1 pod's test lands. Its always-stop: never claim receipts work or production protection before enforce mode.
**Standing note:** per your no-overnight rule, the moment you call it a night I finish the in-flight pods, freeze to PAUSED-STATE, and nothing new launches.
