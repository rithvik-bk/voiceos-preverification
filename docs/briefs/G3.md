# G3 — Core complete + first real metrics
*Written 2026-08-21. Per the compressed calendar this is the Saturday-midday check (arriving early). Program proceeds on the recommendation below unless overruled.*

## 1. What we built (three sentences)
The full Phase-3 core is in: provenance lattice + routing/content split (packages/core), the §20 contract format with a CI lint at 100% write-tool coverage (packages/contracts), the injection firewall with S11.2 transcript-span licensing, the eval harness with three separate corpora + the §18 self-growing fixture generator (packages/eval), and the §13 screen-drift fixture + injection-negative proof carried in the vendored snapshot. The blind adversarial corpus — 40 cases authored from the spec alone, committed before anyone ran it — did its job on the first run: it caught a false-block bug the implementers missed. We fixed the gate (not the corpus), reran, and the numbers below are the after.

## 2. What works, with the commands
```
cd ~/preflight/packages/core     && node --test 'test/**/*.test.ts'   # 16/16 (incl. no-LLM/no-deps enforcement)
cd ~/preflight/packages/contracts&& npm test                          # 13/13, COVERAGE 100.0%
cd ~/preflight/packages/eval     && node --test                       # 20/20; then src/cli.ts for per-corpus tables
cd ~/preflight/vendor/uvo        && npm test && node tools/preflight-eval.mjs  # 427/427, zero-LLM, 0.25/0.31ms
```

## 3. The metrics — honest, per corpus, never blended
| Corpus | Result | Honest caveat |
|---|---|---|
| **Blind** (40, spec-only, pre-committed) | runnable 5/40; false-block **5/5 → 1/5** after the RRP fix | 35 not-runnable-yet (capabilities the skeleton lacks — each NAMED, none dropped). 0 BLOCK-expected cases runnable yet, so catch-rate is n/a today, stated as such. The 1 remaining false-block (B38) is a SEPARATE gap (tier-blind lattice), not a regression. |
| **Replay** (their community bugs) | 5/5 caught, machine codes matched | 5 verbatim + 6 reconstructed, labeled. R1 (calendar/stale-screen headline) is not-runnable in the pure eval path (needs screen+time) — demonstrated instead by the dedicated §13 fixture (green). |
| **Self** (regression) | 7/7 | 100% by construction — labeled by-construction, never presented as a discovery. |
| **Latency** | 0.25ms allowed / 0.31ms blocked (vendored gate); 7.1µs core skeleton compute | In-path compute only; p95/p99 incl. re-verify network = UNMEASURED (needs their platform). |

## 4. The decision
**Recommendation: proceed to Phase 4/6 wiring (demo + docs), close B38 as one small pod along the way.** Core is complete and the one open false-block (B38: a Tier-1 read grounded from rank-2 known state needs tier-aware min-rank) is low-harm — it false-blocks a READ, which fails-open by policy (S19) anyway — and is a ~1-file lattice fix. It should be closed for cleanliness, but it does not gate the demo.
The bigger Saturday question this surfaces: **the blind corpus's value IS the pitch.** "A corpus written against only my spec, committed before I ran it, caught 5 of my own false-blocks and I fixed 4 with a rule the spec already required" is a stronger Kai-facing story than any catch-rate number. Recommend leading the rigor half of the demo with exactly that.

**Rejected option:** chase the 35 not-runnable blind cases to raise the runnable fraction before Sunday. That means building screen/streaming/amount capabilities the platform doesn't even expose to us — days of work for a number that's honestly captioned "harness-simulated" either way. Not worth it; the honest not-runnable list is itself credible.

## 5. What changes if you overrule
If you want B38 fixed BEFORE any demo wiring, say so and it goes first (costs ~one pod, delays demo wiring by that much). If you want a blind catch-rate number on a slide, we'd have to build simulated capabilities and caption them as simulated — I recommend against.

## 6. DEMO-READY status
Strong. Act 1 (injection blocked, provenance type error on screen) and Act 2 (drift caught + one-tap repair) both have working machinery + fixtures. Act 3 = replay corpus 5/5 + the blind-corpus story + per-tool offender ranking (contracts telemetry). The three-act DEMO pod + the README are the remaining build; the FOUNDER-BRIEF (20 Q + crib) is already written for Saturday rehearsal.

## Next
DEMO pod (three-act stage script + rehearsal) + B38 fix + README/quickstart from clean checkout. Then G4+G5 Saturday evening (blind number + which numbers lead), G6 = Saturday-night dress rehearsal.
