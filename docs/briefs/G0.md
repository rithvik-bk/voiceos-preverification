# G0 — Recon & feasibility gate
*Written 2026-08-21 (Friday night). Program halted awaiting Rithvik per Rule 10.*

## 1. What we built (three sentences)
The program skeleton is live: repo at `~/preflight/`, all 21 subagent definitions with veto conditions (REDTEAM-BLIND isolation enforced in its definition), and the full docs spine (STATE, CLAIMS, DEMO-READY, UNKNOWNS, COMPLETENESS). RECON verified ground truth with fresh evidence: the v3 gate runs — **416/416 tests, 50/50 drifts caught, 0/6 false blocks, 0.24 ms — re-run tonight**, and the gate core (PreflightBlock, grounding store, resolver, time resolver) is cleanly portable from the Slack adapter. SCOPE produced the de-scope ladder, the §1–§32 completeness table, and a compressed gate calendar.

## 2. What works, with the command
```
cd /Users/rithvik/universal-voiceos-oauth && node tools/preflight-eval.mjs && npm test
```
50/50 caught pre-write, 0 false blocks, 416 tests green (exit 0, tonight).

## 3. What broke or surprised us
- **Test count is 416, not 415** — quote 416 from now on.
- **One §26 overclaim caught:** the schedule *undo handle* exists (`tools-t2.ts:904`) but `slack_undo_scheduled` is unbuilt. Never say "undo works" until it does. (This is exactly why RECON exists.)
- **Act 3 as specified is impossible before Sunday:** "shadow mode over their real traffic" needs access we don't have. Pivot: run the gate over the **replay corpus of their own community-reported bugs** — 11 candidates, 5 verbatim (headline: their calendar end-time bug with a verified stale-screen root cause — a §13 screen-drift case we catch by design). Note: **no Spotify misfire exists in local sources** — that seed claim was mine and is logged as UNKNOWN U8, not cited.
- **No streaming surface exposed to integrations** (no token ids, no revision events, no word confidence — all confirmed-by-absence, tagged ASSUMPTION). §6–§8 therefore run on a **simulated decoder feeding the real gate** — proven in harness, stated honestly as such.

## 4. The decision you need to make
**Approve the compressed program (recommended):**
1. **Gate calendar → four touchpoints:** G0 = this brief (10 min). G1+G2 merged = one async brief tonight (spec lock + walking skeleton shape). G3 = Saturday midday (10 min). G4+G5 = Saturday evening (15 min: the blind number + which numbers lead). G6 = the Saturday-night dress rehearsal you already owe.
2. **Proceed-on-recommendation default** between touchpoints, with four always-stop conditions: any spec expansion, any change to a demo act, any proposed runtime dependency, any non-negotiable red-team finding.
3. **Port, don't rewrite:** `packages/core` wraps/extracts the proven v3 gate; the 416-test Slack integration (Rithvik's personal project, not VoiceOS's product) remains the production adapter — production for him.
4. **Sunday-LIVE set** (SCOPE's ladder): walking skeleton (§1/§2/§5) + shadow mode & coverage counter (§19) + transcript-span enforcement / Act 1 (§11) + blind & replay corpora with per-corpus numbers (§18) + Slack schema annotations & lint (§20) + constrained re-emit (§4) + receipts-lite (§17) + per-tool offender ranking / Act 3 (§21). PROVEN-IN-HARNESS: §6 revision invalidation, §10 TTL, §16 reversibility classes. Screen/speculation (§8, §13) stay modeled/specified. Falls first if time runs out: §8 property test → specified-only, then §16 → modeled, then §10 → specified-only. Never falls: skeleton, shadow, blind corpus, Act 2.

**Rejected option:** run the six gates sequentially as written. Each stop idles the program for hours you don't have; the predictable result is Phase 3 unfinished Sunday morning.

## 5. What changes if you say no
Gates run as written; expect the demonstrated system Sunday to be roughly today's DEMO-READY plus a walking skeleton, with the blind corpus unscored. Spec unchanged either way (Rule 14).

## 6. DEMO-READY status
Non-empty since day zero: the deterministic gate already runs inside his own Slack integration (personal project on their platform) — the command in §2 is the demo of record until the skeleton supersedes it.
