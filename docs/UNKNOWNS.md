# UNKNOWNS
*Every ASSUMPTION about VoiceOS, ranked by demo blast radius. Populated by RECON 2026-08-21 from `pods/recon-inventory.md`; orchestrator maintains. Source of truth for platform facts: `~/voiceos-intel/` (byte-cited RE brief, dated 2026-08-13).*

| # | Assumption | Demo blast radius | How to verify | Fallback |
|---|---|---|---|---|
| U1 | No streaming transcript (token ids, interim/revision events) reaches integrations — revisions exist client-side only (Deepgram `interim_results=true` in main.js) and die before the MCP boundary | **HIGH** — §6-8 streaming story cannot run live on VoiceOS | Ask Jonah/Kai directly at the visit; or instrument a dummy MCP server and log everything VoiceOS ever sends it during a spoken command | Simulated decoder: our demo adapter replays a timed interim/final/revision event stream into the real gate; label it "simulated ASR, real gate" on the slide |
| U2 | We can never obtain VoiceOS production traffic/logs for shadow mode before Sunday | **HIGH** — kills §27 Act 3 as specified ("their own numbers") | Only they can grant it; ask in the room (it is also the natural "week 1 as intern" ask — see §32) | Act 3 = replay corpus of their community's publicly reported failures (docs/pods/replay-corpus-candidates.md) + our measured eval table |
| U3 | There is no platform-authoritative "user's exact spoken words" delivered per tool call — the `said` arg is model-filled (the LLM copies the transcript into it) | **MED-HIGH** — transcript-rank provenance rests on the model cooperating; a founder will ask | Dummy-server instrumentation (see U1): check whether any transcript field arrives outside the model-filled args | State it plainly: "today the model attests, the gate verifies internal consistency; the platform injecting the transcript is the one-line RFC ask" (§22) |
| U4 | No pre-execution hook exists for third parties — the only interception point we own is inside our own MCP server's tool handlers | **MED** — gate demo must scope to our own integration's tools | Confirm with founders; also check whether the server-side 8-state machine (`deep/19`, CONFIRM_REQUIRED state) has any extension point | Demo on our tools; pitch platform-level Preflight as the adoption hook (§28), which is the plan anyway |
| U5 | Word-level ASR confidence is not forwarded to the agent or integrations (client parses Deepgram `alternative`s but nothing shows confidence crossing any boundary) | **LOW-MED** — §14/§15 confusion-set features stay harness-only | Ask Kai (he owns accuracy-vs-latency); dummy-server instrumentation cannot see this (it is upstream of MCP) | Confusion sets built from a phoneme matrix (already the §26 roadmap position); no demo dependency |
| U6 | No turn-boundary/barge-in or screen-transition events are exposed to integrations; screen reads are one turn lagged even natively (`C1-browser-control-limits.md:55`) | **LOW-MED** — §9/§12-13 remain modeled-in-core, proven in harness only | Ask at visit; the lagged-screen fact is already verified (it is their calendar bug's root cause — quote it, don't ask it) | Harness fixtures; the calendar end-time bug becomes the motivating replay case |
| U7 | The platform contract (manifest schema, MCP-stdio shape, card bridge) is unchanged since the intel brief was cut on 2026-08-13 | **MED** — a silent schema change breaks the live demo on their machine | Re-run a folder install of the current Slack integration against today's VoiceOS build before the visit; diff `voiceos.integration.json` handling | Pin the demo to our own laptop + our known-good VoiceOS version; bring the video as backup |
| U8 | Discord-reported "Spotify misfire with no confirmation card" exists | **LOW** — only affects one replay-corpus row | Search the full Discord export again / ask Arav for the message link | Not found in ~/voiceos-intel this session; the corpus stands at 5 verbatim cases without it — do not cite a Spotify case unless a verbatim source surfaces |
| U9 | Native actions (osascript Calendar/Spotify/Mail path in main.js) bypass the manifest confirmation-card contract entirely | **LOW** for demo, **HIGH** for the pitch narrative ("the calendar bug had no card because native actions have no cards") | Trigger a native calendar write on a test Mac and observe whether any confirmation card appears | If unverified by Sunday, phrase as "the reported calendar bug shipped a wrong end-time with no visible catch" — true per the report without asserting the card mechanism |
| U10 | Their server-side agent uses the tool `description` as the only routing signal (no vector index) — "lazy hierarchical dispatch, one proxy tool per integration" | **LOW** — routing is Arav's contested lane, not Preflight's demo | Kai conversation; or observe `list_integrations` calls via dummy-server logging | None needed; Preflight measures outcomes, not routing internals |
