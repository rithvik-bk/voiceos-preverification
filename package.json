{
  "name": "preflight",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Preflight — a deterministic PRE-verification layer for voice-agent tool calls. Before any action fires, every routing parameter (who/where/when/how-much) must trace to a licensed source; screen/content never licenses routing (the injection firewall). Zero LLM in the hot path.",
  "workspaces": [
    "packages/*",
    "packages/adapters/*"
  ],
  "engines": {
    "node": ">=23.6"
  },
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "bench": "npm run bench --workspace @preflight/core"
  },
  "devDependencies": {
    "@types/node": "^24.13.3",
    "typescript": "^5.7.3"
  }
}
